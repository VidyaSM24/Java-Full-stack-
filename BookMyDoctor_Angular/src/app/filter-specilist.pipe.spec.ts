import { FilterSpecilistPipe } from './filter-specilist.pipe';

describe('FilterSpecilistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpecilistPipe();
    expect(pipe).toBeTruthy();
  });
});
